/**
 * @module Service
 * @description This is a file that holds the business logic for greeter model.
 * 
 * What should be in here:
 * 1. Use validated data from routes file.
 * 2. Manipulate database models in here.
 * 3. Return error if you want to throw an error to client.
 * 4. Data returned here is passed on to client.
 */

export const ping = () => {
    return "Greeter Service Pinged Successfully"
}